import React from "react";

export default function Buttons(){
    return(
        <div>
            <button style={{marginRight:10}}>Pay Now</button>
            <button>Cancel</button>
        </div>
    )
}